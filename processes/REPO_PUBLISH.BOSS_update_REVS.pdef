# This process is part of the REVS Monitoring code
#
# It should be linked to projects where REVS is tracking the packages
# and versions.

Ruote.process_definition 'BOSS_update_REVS' do
  sequence do
    echo 'feed revs'
    notify_irc :irc_channel=> '${irc_channel}', :msg => 'Feed REVS'
    feed_revs
  end
end
