I have made the following changes intended for :
#for $action in $req.actions
  $action.target.project / $action.target.package
#end for

Please review and accept or decline.
BOSS has already run some checks on this request.
See the "Messages from BOSS" section below.

${f.obsurl}/request/show/$f.ev.id

Thank You,
$f.author.realname ($f.author.email)

[This message was auto-generated]

---

Request # $f.ev.id:

Messages from BOSS:
#for $mesg in $f.msg
     $mesg
#end for

State:   $req.state.name          $req.state.when $req.state.who

Reviews:
#for $review in $req.reviews
     $review.state by $review.by_user : $review.comment
#end for

Changes:
#for $action in $req.actions
  $action.type: $action.source.project / $action.source.package -> $action.target.project / $action.target.package
  $action.diff
#end for
